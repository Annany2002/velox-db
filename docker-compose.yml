services:
  velox-db:
    build: .
    container_name: velox-db-server
    ports:
      - "6380:6380"
    volumes:
      - velox-db-data:/data
    restart: unless-stopped
    environment:
      - VELOX_AOF_PATH=/data/velox-db.aof
volumes:
  velox-db-data:
